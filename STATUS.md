TS=2026-09-04T04:04:43Z | TASK=GO_P9 | STATE=TESTING | VERSION=5598d86e | NOTE=من tail الإنتاج: محاولتا المتصفح النظيف أنشأتا الطلب ثم رُفضت أول قراءة handoff بـ409 DIFFERENT_REQUEST_ACTIVE لأن مساحة العمل نفسها كانت تحمل جذر طلب أقدم (أحدهما READY_FOR_CONNECTION بلا جلسة منذ 01:07 والآخر BLOCKED_SESSION)، والشاشة بقيت على الخريطة بلا جملة — هذا هو «3 دقائق ثم طوّلنا شوي». المنشور: (5) الطلب الأقدم الخامل يُحرَّر تلقائيًا عبر مسار الإلغاء نفسه (الذي يرفض إلغاء إرسال حيّ) ويُعاد الإنشاء/التأكيد مرة، والحيّ فعلًا يُتبنّى، وأي فشل تأكيد يُقال على شاشة البحث مع «ابدأ من جديد». (1)(2)(3) قياس زمن البحث وتسريعه ≤20 ث والتدفق من أول مزود: workflow القياس يعمل الآن (4 خرائط + بناء الأنيميشن) | NEEDS_OWNER=إعادة محاولة /go من نفس المتصفح النظيف بعد النشر؛ الاقتران حتى CONNECTED (لم يصل أي نداء اقتران إلى الإنتاج بعد)
TS=2026-09-04T04:26:03Z | TASK=GO_P9 | STATE=DEPLOYED | VERSION=6ad11787 | NOTE=Search leg parallelised (Serper phrasings, organic, page fetches together), provider commit in concurrent chunks, widening rungs time-gated, /go polls ≤1.5 s, wizard streams providers as found; measuring after-times on the next real search | NEEDS_OWNER=none
TS=2026-09-04T04:31:36Z | TASK=SEARCH_ANIMATION_V2 | STATE=DONE | VERSION=6ad11787 | NOTE=Search-journey animation live on /go (Maps → Search → TikTok → Haraj, real typed queries, 4-source progress bar, rotating captions, ends with results); 375px screenshot and 10 s video captured on production | NEEDS_OWNER=none
TS=2026-09-04T04:56:19Z | TASK=MOBILE_TEST_3_1_2 | STATE=DEPLOYED | VERSION=4850ac9e | NOTE=Home page shows تسجيل الدخول / حسابي and a direct طلباتي link at every width (was CSS-hidden under 620px); طلباتي opens standalone for a signed-in customer from home, menu and /go; sign-in form no longer bounces a /go anonymous identity | NEEDS_OWNER=none
TS=2026-09-04T05:17:53Z | TASK=PAIRING_404_REALFIX | STATE=DEPLOYED | VERSION=b1e5edd7 | NOTE=Worker keeps a pairing row alive across the bridge session window so a CONNECTED callback matches (204) instead of 404-then-device-logout; late CONNECTED within a grace still applies; terminal-on-missing is 410 not 404. Owner round showed the bridge sent NO CONNECTED callback at all (only the terminal DISCONNECTED), so the connect never registered on the bridge side | NEEDS_OWNER=set Render BRIDGE_COMPANION_NAME=Chrome and restart, then re-pair — the دبّر device-name identity is the suspected cause of the missing CONNECTED
TS=2026-09-04T05:17:53Z | TASK=MOBILE_TEST_3_3 | STATE=DEPLOYED | VERSION=b1e5edd7 | NOTE=6A latency: navigation keeps the app frame instead of a blank loader, build-freshness probe moved off the critical path, per-request identity cache, filter taps repaint from the list cache, account page reads parallelised | NEEDS_OWNER=none
TS=2026-09-04T05:24:34Z | TASK=BRAND_HEADER_FOOTER | STATE=DEPLOYED | VERSION=eaa06226 | NOTE=Sticky brand strip (logo + دبّر يدبّرها) on every page and a footer with the same mark plus الشروط/الخصوصية/تواصل; 375px verified, no content shift, no horizontal scroll; landing header stacks below the strip | NEEDS_OWNER=none
TS=2026-09-04T05:24:34Z | TASK=GO_RESUME_FIX | STATE=DEPLOYED | VERSION=eaa06226 | NOTE=A refresh after a successful /go search restores the same step with its found providers from sessionStorage instead of losing them; a background re-fetch never bounces the customer back to the first screen | NEEDS_OWNER=none
TS=2026-09-04T05:34:16Z | TASK=GO_429_FIXED | STATE=DEPLOYED | VERSION=6df1cbc1 | NOTE=Anonymous daily search cap raised to 100 for launch testing (revert to 5 before public launch, noted in MORNING_NEEDS); a reached cap now shows a clear sentence plus a sign-in button instead of a breakage line, and the client code now matches the code the route emits; signed-in path verified uncapped | NEEDS_OWNER=none
TS=2026-09-04T05:47:25Z | TASK=PAIRING_ROOT_CAUSE | STATE=BLOCKED | VERSION=bridge-c6bcd5d | NOTE=f2f3d5f switched session-end from LOGOUT to DISCONNECT_ONLY (default); DISCONNECT_ONLY never unlinks the companion, so a day of test sessions filled the phone's WhatsApp linked-device list and WhatsApp then rejected every new pairing code at entry. makeWASocket options are byte-identical to yesterday's working build (browser tuple with COMPANION_NAME=Chrome equals Browsers.macOS Chrome). Bridge fix pushed: default back to LOGOUT + logs the browser tuple at socket creation | NEEDS_OWNER=on the phone remove the stale دبّر/Chrome(Mac OS) linked devices; on Render set BRIDGE_SESSION_END_MODE=LOGOUT and restart (or deploy bridge c6bcd5d), then re-pair
TS=2026-09-04T06:12:21Z | TASK=PAIRING_DISK_CLEAN | STATE=BLOCKED | VERSION=bridge-4136dc3 | NOTE=After LOGOUT + empty devices the code is accepted and the transport opens but no CONNECTED, no rejection — the flow stalls after CONNECTION_STATE and the pairing lifecycle logs were STAGING-only, so production showed nothing. Bridge now logs every post-open step on production (awaiting-registration, registered-observed, completion-rejected-past-window with timestamps, CONNECTED-enqueued) and reports priorRegisteredCreds/reconnect at socket creation, so a stale-disk contamination names itself | NEEDS_OWNER=deploy bridge 4136dc3 on Render, re-pair once, and send the new log lines after CONNECTION_STATE (they will name the exact stuck step)
TS=2026-09-04T17:10:47Z | TASK=MOBILE_TEST_4_FIXED | STATE=DEPLOYED | VERSION=204aef70 | NOTE=Cancel is now atomic — it retires the execution's own heavy slots (fenced) and force-releases the customer's own root (fenced, owner-scoped) instead of declining and leaving a half-cancel; the only decline is a real in-flight send. Because cancel converges, a stuck request auto-releases on a new /go with no manual cancel. Blocker message is now actionable | NEEDS_OWNER=none
TS=2026-09-04T17:11:38Z | TASK=BRIDGE_PUSHED_4136dc3 | STATE=DONE | VERSION=bridge-4136dc3 | NOTE=Confirmed origin/codex/dabbir-render-staging-bridge-v1 HEAD = 4136dc3052ae33b934b0c4b239f3cd8cdd8a9670; both c6bcd5d (LOGOUT default) and 4136dc3 (production pairing diagnostics) are on that branch above f2f3d5f. Render is still Live on f2f3d5f because its deploy has not picked up the new commits, not because they are absent from origin | NEEDS_OWNER=on Render, Manual Deploy → Deploy latest commit (or enable auto-deploy for this branch), then re-pair and send the log lines after CONNECTION_STATE
TS=2026-09-04T22:59:32Z | TASK=PAIRING_DISK_CLEAN_TOOLING | STATE=BLOCKED | VERSION=bridge-e00c536 | NOTE=Correction: AUTH_MATCHED_CURRENT is the bridge's inbound API-key auth log (server.mjs:352, logged after authorized() passes on every valid request) — a caller hits the bridge each minute, NOT a stale session re-authenticating on disk; wiping the disk does not change it. A wipe is still a fine clean-slate pre-launch. Storage path is /data/sessions; all durable state under it is dot-prefixed, so a glob wipe skips it. Added opt-in BRIDGE_WIPE_SESSIONS_ON_BOOT (bridge e00c536) that removes only hex-uuid session dirs at boot. 94cb151 lacks diagnostics, so a stall after CONNECTION_STATE is silent there | NEEDS_OWNER=Render Shell on the official bridge: `rm -rf /data/sessions/*` then restart (removes only session dirs, keeps callback/replay state); OR deploy bridge e00c536 with BRIDGE_WIPE_SESSIONS_ON_BOOT=true for a self-clean + production pairing diagnostics, then re-pair
TS=2026-09-04T23:03:51Z | TASK=READY_TO_DEPLOY_4136dc3 | STATE=DONE | VERSION=bridge-4136dc3 | NOTE=4136dc3 is Worker-compatible exactly like 94cb151: contract DABBIR_BRIDGE_CONTRACT_V1 unchanged, CONNECTED callback payload {status,connectedAt,sessionExpiresAt} unchanged, waWebVersion accepted, no runtime code-digest check; the only diff vs 94cb151 is config+logging+session-end-default+tests (the two contract-adjacent diff lines are inside diagnostic LOGS, not the API). Session-end default is LOGOUT (not DISCONNECT_ONLY). Search and send are not affected | NEEDS_OWNER=deploy bridge 4136dc3 on Render, try one pairing, and send the log lines after CONNECTION_STATE (awaiting-registration / registered-observed / completion-rejected-past-window / CONNECTED-enqueued + priorRegisteredCreds)
TS=2026-09-05T01:01:40Z | TASK=PAIRING_BROWSER_FIXED | STATE=DONE | VERSION=bridge-b6c63c8 | NOTE=Proven cause from e00c536 diagnostics: browser[1]="دبّر" in the Baileys tuple made WhatsApp reject the companion at code entry (priorRegisteredCreds:false, devices empty, LOGOUT set — the name was the only variable), and BRIDGE_COMPANION_NAME never reached the process (log showed the default). Reverted the tuple to the exact 94cb151 identity Browsers.macOS("Chrome") — hardcoded, no env dependency — so browser[1] is a real browser token; companionBrowserIdentity removed. Linked device shows "Chrome (Mac OS)". Bridge tests green (176/177, the 1 is pre-existing) | NEEDS_OWNER=deploy bridge b6c63c8 on Render, confirm the socket-identity log now prints browser ["Mac OS","Chrome","14.4.1"], then re-pair — CONNECTED should arrive and dispatch begin
TS=2026-09-05T01:08:14Z | TASK=SEARCH_CLASSIFY_2_FIXED | STATE=DEPLOYED | VERSION=ce3749b0 | NOTE=REQ-7B7F8EC found 12 providers but showed a failure — a two-writer race left purchase_requests.status=FAILED after markReady healed only product_phase. Fixes: markReady heals stale FAILED→DISPATCHING; markFailed no-ops when providers exist; "اديتر/ادايتر" added as auto-parts catalog aliases so it classifies as قطع غيار سيارات; insufficiency card leads with "وضّح طلبك أكثر" not the misleading scope message. 21 targeted + suite green (only pre-existing failures) | NEEDS_OWNER=re-run "اديتر سيارة" on /go — it should classify as قطع غيار سيارات and show ≥5, no false failure
TS=2026-09-05T01:12:12Z | TASK=SEARCH_WIDEN_LIVE | STATE=DEPLOYED | VERSION=1961fbe2 | NOTE=The sufficiency ladder now keeps widening toward the full target (up to 20) instead of settling around 14 — the GO_P9 8s expansion window was raised to 16s, so with the parallel Serper path it buys more alternative-category/neighbouring-district phrasings before declaring the result complete, and still breaks the moment kept reaches the target. A market with genuinely fewer than 20 is unaffected | NEEDS_OWNER=none
TS=2026-09-05T01:40:31Z | TASK=SEND_STABLE | STATE=DEPLOYED | VERSION=c255d0df | NOTE=Production D1 showed the send hung at DISPATCHING sent=1 of 16 — a message DID go out, then the SELF continuation hop died on the post-CONNECTED 515 STREAM_RESTART and the sweep only recovered READY_FOR_CONNECTION jobs, so the DISPATCHING job was orphaned. The sweep now also resumes a DISPATCHING job idle >60s (one pace + continuation sleep + grace), retryable-guarded, so a dropped continuation self-heals and the batch continues to N of M. The live stuck send is now progressing (sent=2 of 16, CONNECTED, session live) | NEEDS_OWNER=watch the send continue; if it stalls, the sweep resumes it within the cron interval. PROVIDER_EVENT ERROR = the bridge safe-logger's fallback for an unclassified Baileys error (a failed send), retried automatically
TS=2026-09-05T01:49:59Z | TASK=SEND_PACING_FIXED | STATE=DEPLOYED | VERSION=dbe70934 | NOTE=Pace dropped 12s→5s with ±2s jitter for all 20 providers (owner tested 10 sends today, no ban); no fast/safe choice, no customer warning; STREAM_RESTART handled by retry + the stalled-DISPATCHING sweep, first-send grace removed. ~20 providers in ~100s | NEEDS_OWNER=run a full send and confirm all messages out within ~2 min
TS=2026-09-05T02:20:00Z | TASK=SEARCH_PRECISION_LIVE | STATE=DEPLOYED | VERSION=1e070a1fe277 | NOTE=The owner's report — ABS search returned general workshops, split-AC returned generic "صيانة", parts returned "سيارات" — traced to two spots: the Maps query was built from the broad category name only, and the final sort used rating alone so a high-rated general shop outranked a lower-rated specialist. Now requestedSpecialty() pulls the literal trade (ABS/سبليت/تشليح/اديتر) from the customer's words and the Maps query carries it ("متخصص فرامل ABS الرياض" / "إصلاح …" / "ورشة …"); suitabilityRank orders specialist-first (fit before rating) whenever a specialty was asked; the SPECIALTY_MATCH/RATING/PROXIMITY/SUITABILITY breakdown is persisted and shown per request in admin. /go now offers nine Riyadh region buttons + optional precise district; cross-region widen still gated by radiusExpansionConsented. Suite green (only pre-existing failures) | NEEDS_OWNER=re-run "صيانة ABS الرياض شمال" / "فني تكييف سبليت جدة" / "اديتر كامري الرياض" — each should return actual specialists, not general shops
TS=2026-09-05T02:45:00Z | TASK=UNIFY_FLOW_LIVE | STATE=DEPLOYED | VERSION=864fdad774f2 | NOTE=Retired the second request-writing frontend. There were two copies of the journey — anonymous /go and a signed-in composer at /requests/new — which is why a fix in one left the other broken. The backend was already one path (both called POST /api/requests, which attaches to whoever is authenticated), so this was a frontend unification: landing "ابدأ طلبك" and every CTA open /go directly; the landing starter drops the typed request in the shared draft and hands into /go with no signup wall; /go seeds need+message from that draft then clears it; /requests/new is now a 307 redirect to /go; every internal "new request" link (nav, dashboard, billing, request-detail, zero-send failure) points at /go; the dashboard's two start tiles collapse to one honest entry (the old voice tile promised a mic /go lacks). Signed in → request attaches to the account; anonymous → stays anonymous; registration is optional (only to save/follow requests). Verified on production: 3 CTAs → /go, 0 /requests/new in landing HTML, /requests/new → 307 /go, and a landing draft carried into /go's need field then cleared (draftConsumed). tsc=0, build guards PASS, full suite at baseline (zero net-new failures). The signed-in composer component survives only for the local review harness and can be deleted in a follow-up | NEEDS_OWNER=walk it once on your phone — from the homepage: اكتب طلبك → دوّر لي مزودين → بحث → ربط واتساب → إرسال، بلا أي تسجيل. Voice input existed only in the retired composer; want it ported into /go as a separate task, or drop it?
TS=2026-09-05T03:05:00Z | TASK=OWNER_CUSTOMER_DETAIL | STATE=DEPLOYED | VERSION=949734e8ebde | NOTE=New admin page /control/[accessKey]/customers/[id] shows one customer whole: identity + the WhatsApp number their messages send from (customer_phone_identities, live first) + plan + free allowance (request_credit_accounts/reservations: granted/consumed/reserved/released/remaining) + every request with provider count, reply count, and a per-provider send-status label (rp.status: أُرسل/وصل/جارٍ الإرسال/فشل …) reusing the same dashboard and execution-shortlist joins narrowed to one user_id. Guarded by the existing requirePlatformAdminPage; notFound() for an unknown id. Console customer rows now link into it. Also corrected a UNIFY_FLOW route-contract pin the compiled-HTTP e2e had masked: /requests/new is a permanent 307 to /go for everyone (verified authed + anon), not a sign-in-gated surface. tsc=0, build PASS, full suite at baseline (57, zero net-new). Retired composer new-request-composer.tsx logged for deletion after /go coverage is confirmed; voice-to-/go deferred to after the queue | NEEDS_OWNER=open a customer from غرفة التشغيل → «العملاء والاستخدام» → اضغط اسم العميل، وتأكد أن الرقم والطلبات وحالة الإرسال وعدد الردود تظهر صح
TS=2026-09-05T04:05:00Z | TASK=DEEP_UNDERSTANDING_LIVE | STATE=DEPLOYED | VERSION=727b4079a35f | NOTE=Fixed the core "قاعة حفلات → منظّمين" bug. Verified root cause by live repro: classification was pure keyword matching with NO venue category and no place/service/product dimension, so a hall fell through to the events=organizers category, and that wrong category (not the model) built the Serper query. New src/modules/ai/deep-understanding.ts classifies each request as PLACE|SERVICE|PRODUCT|ONLINE_STORE + a precise searchSubject ("قاعات أفراح") via a cheap model (gpt-4o-mini, 20 few-shot examples, no training) with a deterministic rule-based fallback that alone handles the owner's test cases. The searchSubject+intentType now DRIVE the query: effectiveSearchSubject prefers them and buildMapsQueries shapes phrasings by type (venue nouns for PLACE, shop for PRODUCT, delivery for ONLINE_STORE) — proven by tests that a WRONG category no longer leaks "تنظيم/منظم". Cost ≈ $0.0003/request (~$0.33/1k, ~$3.25/10k); understanding ≤2.5s then falls back to instant rules. tsc=0, build PASS, full suite at baseline (57), +11 tests | NEEDS_OWNER=re-run "قاعة أفراح الرياض شمال" و"قاعة زواجات" و"اديتر كامري" و"متجر عطور أونلاين" — should return the right TYPE (halls not organizers). If you want the model path (not just rules) confirm gpt-4o-mini is enabled on the OpenAI key, else set OPENAI_UNDERSTANDING_MODEL to a model you have. Clarify buttons (AI_CONVERSATION) next
TS=2026-09-05T04:45:00Z | TASK=MODEL_UPGRADED | STATE=DEPLOYED | VERSION=76ba81aeee32 | NOTE=Understanding model raised gpt-4o-mini→gpt-4o (OPENAI_UNDERSTANDING_MODEL=gpt-4o), owner's quality-over-cost decision. VERIFIED the model path really fires on production (not rules fallback) via a token-guarded flag-gated read-only canary that ran understandRequest inside the worker for 10 requests × [mini, 4o]: fired 10/10 on gpt-4o. Both models understood NEW unexpected requests the rules can't — "أبي أحد يعلّمني عود"→SERVICE/مدرسون لتعليم العود، "أبي كوفي أذاكر فيه"→PLACE/كافيهات، "أبي أحد يصلح دراجتي"→SERVICE/فنيو تصليح دراجات، "أبي أحد يخيط لي ثوب"→SERVICE/خياطون، "قاعة زواجات"→PLACE/قاعات أفراح وزواجات، "متجر عطور أونلاين"→ONLINE_STORE، "حفلات"→يسأل(؟). gpt-4o gave cleaner subjects + zero timeouts (mini fell back once), so adopted. Timeout raised 2.5s→5s so the model result is used not the rules floor. Latency observed ~0.9–2.1s/call. Canary left DISABLED post-verification (re-enable via UNDERSTANDING_CANARY_ENABLED). tsc=0, build PASS | NEEDS_OWNER=none — model path confirmed working live. Next: AI_CONVERSATION
TS=2026-09-05T05:20:00Z | TASK=AI_CONVERSATION_LIVE | STATE=DEPLOYED | VERSION=d9337edd5203 | NOTE=Understanding became a short conversation. When the model is genuinely unsure of the TYPE (bare "حفلات"=قاعة أم منظّم؟ / "عطور"=محل قريب أم أونلاين؟) the create route returns 422 UNDERSTANDING_CLARIFICATION_REQUIRED with ONE buttoned question BEFORE searching; /go renders a CLARIFY step (reusing the global .clarification-card/.clarification-options styles), records the tap, and resubmits — no typing, capped at 2 questions, and clear requests never pause. Once answered, understandRequest resolves the type (rule-based resolves deterministically so it never re-asks) and search runs with the precise subject. Fires even though /go pre-confirms (the model decides ambiguity, not a checkbox). Registered understanding-canary in the route authz manifest. tsc=0, build PASS, full suite at baseline (57) | NEEDS_OWNER=on /go type «حفلات» (should ask قاعة/مكان vs شركة تنظّم) و«عطور» (محل قريب vs متجر أونلاين), tap an answer, confirm it searches the right type; a clear request like «قاعة أفراح» should NOT ask
TS=2026-09-05T06:10:00Z | TASK=EDIT_AND_ATOMIC_CANCEL_LIVE | STATE=DEPLOYED | VERSION=76a19a9a9986 | NOTE=Item 3 of the official queue. Cancel was non-atomic: it flipped the request to CANCELLED then ran external cleanup + terminalizers + a convergence gate that each THREW on a precondition, leaving the customer's root reserved ("ما قدرنا نكمل الإلغاء") so they couldn't start a new request. Now after the flip everything is best-effort: external provider cleanup, terminalization, and root release are each wrapped (log+defer to background reconcilers, never abort); the throwing gate is replaced by a CANCELLED success (root freed = customer unblocked, residues converge async); the ONE decline is a genuine in-flight send → REQUEST_NOT_CANCELLABLE (the code /go maps to "adopt the live request"). Added "عدّل طلبي" on the results screen: returns to the first screen with the text preserved and frees the abandoned request via the now-reliable cancel. MOBILE_TEST_4 + terminalization module tests green; full suite at baseline (57); +4 tests | NEEDS_OWNER=on /go after results, tap «عدّل طلبي» → should return to your text and let you re-search; and a request that got stuck should now cancel/free cleanly. Next: item 4 FAILED_REQUESTS_LOG
TS=2026-09-05T06:45:00Z | TASK=FAILED_REQUESTS_LOG_LIVE | STATE=DEPLOYED | VERSION=38001ac80ccc | NOTE=Item 4. New failed_understanding table (drizzle/0090, applied to production D1 dabbir-official-controlled-release via wrangler d1 execute --remote, additive CREATE TABLE IF NOT EXISTS) records every request whose understanding needs improving: text, city/district, model intent_type+search_subject, category, confidence, mode, reason, time. recordFailedUnderstanding (best-effort, never affects the request) logs at two points — search route when an unplaceable request finds 0 verified providers (ZERO_RESULTS), and creation when the classifier falls to other-service (OTHER_SERVICE). Admin console → new section "طلبات يحتاج الفهم تحسينها" lists recent misses (type/subject/reason/time). This lets us tune the few-shot instructions from real data with no manual effort. tsc=0, build PASS, full suite baseline (57), +3 tests. Phase A COMPLETE (1-4). | NEEDS_OWNER=none — open غرفة التشغيل → «فهم يحتاج تحسينًا» after some real traffic to see collected misses. Next: Phase B item 5 MULTI_SOURCE + PHONE_EXTRACTION
TS=2026-09-05T07:25:00Z | TASK=MULTI_SOURCE_LIVE | STATE=DEPLOYED | VERSION=126d13851056 | NOTE=Item 5a. Added site: queries by intentType through the Serper /search endpoint we already own (no paid per-platform API): ONLINE_STORE & PLACE & SERVICE → site:instagram.com; PRODUCT → site:haraj.com.sa + instagram; no type → historical generic organic only. Reaches online stores/artisans (Instagram) and used goods (Haraj) that never show in Google Maps. Safe/additive: organic runs only when Maps didn't fill the pool; existing pipeline extracts phone (snippet regex + 1 page fetch) and EXCLUDES any candidate without a sendable mobile, deduped by phone with Maps; organic capped at 5 queries to bound Serper budget. tsc=0, build PASS, full suite baseline (57), +5 tests | NEEDS_OWNER=(1) at 25 sessions + multi-source the Serper daily budget (SERPER_DAILY_CALL_BUDGET, currently 40) will need raising — I'll size it in SCALE_25. (2) Next I build PHONE_EXTRACTION (gpt-4o extraction + measure the real Instagram phone-yield %); if yield <20% I'll recommend relying on Maps+Haraj
TS=2026-09-05T08:00:00Z | TASK=PHONE_EXTRACTION_LIVE | STATE=DEPLOYED | VERSION=136ff7006f89 | NOTE=Item 5b. Measured Instagram site: snippet phone-yield ≈ 5% (1 Saudi mobile of ~21 across "عطور الرياض" + "كوفي هادي الرياض") — below the 20% threshold, because most SA Instagram stores take orders by DM/site and the number lives in the login-gated bio no snippet reaches. Owner decision (recorded): keep Instagram as a near-free bonus (only numbered ones show, already filtered); focus Maps+Haraj as phone sources; add gpt-4o extraction. Built src/modules/ai/contact-extraction.ts: extractContactsWithModel — batched fail-soft gpt-4o /v1/responses (strict schema) returning normalized 9665XXXXXXXX per row where a Saudi mobile exists; rejects +965/landlines; treats scraped text as data. Wired as an injected extractContacts fallback in the organic branch, run only for rows still missing a phone; every downstream gate still requires a valid Saudi mobile so no-number results stay excluded. Its real lift is Haraj bodies + odd formats (wa.me/spaced), not Instagram (can't extract an absent number). tsc=0, build PASS, full suite baseline (57), +3 tests. Phase B COMPLETE except VALUE_RANKING | NEEDS_OWNER=none. Next: item 6 VALUE_RANKING, then Phase C (7 PHONE_IDENTITY, 8 SCALE_25 [needs Render], 9 DELETE_ACCOUNT, 10 bridge [needs your deploy])
TS=2026-09-05T08:20:00Z | TASK=COST_REPORT | STATE=DONE | VERSION=n/a | NOTE=Per-request literal cost. OpenAI = TWO real calls/request: (1) understanding gpt-4o ~1650 in + 130 out ≈ $0.0054 ($0.0035 cached); clarify only re-calls on ambiguity; (2) legacy analyzeServiceSearch gpt-5.6-luna ~700 in + ≤900 out — priciest, output mostly unused (only requiredProviderCapabilities) → biggest saving is DROPPING it (DEEP_UNDERSTANDING replaced it, ~40% cheaper/request, no quality loss); (3) contact-extraction gpt-4o only when organic+missing-phone (~1/3) ≈ $0.006→~$0.002 amortized. Message drafting = no AI (rules+customer text). Serper ~5 calls/request avg (max ~12-15) × ~$0.001 [confirm plan] ≈ $0.005. Cloudflare ~$0.0001 negligible. Render fixed monthly (owner's plan unknown; ~$25-85 for 25 sessions pending LOAD_TEST RAM). Per request: avg ≈ $0.018 (≈ $0.011 if analyze dropped), max ≈ $0.045. Monthly + Render($25): 1k≈$43, 10k≈$205, 100k≈$1,825 ($1,100 if analyze dropped). gpt-4o vs mini: both hit type 10/10, mini ~18x cheaper — but the real lever is dropping the legacy call, not the model | NEEDS_OWNER=(1) current Render plan+price (2) Serper plan price (3) gpt-5.6-luna price from OpenAI dashboard, or approve dropping analyzeServiceSearch to remove it. Continuing to item 6 VALUE_RANKING
TS=2026-09-05T08:45:00Z | TASK=VALUE_RANKING_LIVE | STATE=DEPLOYED | VERSION=b2e46964052e | NOTE=Item 6. "المناسب" redefined: providerValueScore rewards hidden gems (strong rating + credible-but-modest reviews, +0.3), discounts the famous (≥400 reviews -0.25, ≥200 -0.1), and discounts uncredible ratings (<3 reviews, -0.6) so a lucky 5★/1 can't lead. suitabilityRank keeps specialty fit first, then value, then fewer-reviews tiebreak; qualityRank uses value score too. Net: fanout is a diverse mix surfacing hidden gems ahead of but alongside the famous — not just the well-known. tsc=0, build PASS, full suite baseline (57), +4 tests (updated the old more-reviews-wins pin to the new hidden-gem-leads behavior). Next: COST_OPTIMIZE (drop legacy gpt-5.6-luna call) then Phase C | NEEDS_OWNER=none
TS=2026-09-05T09:15:00Z | TASK=LEGACY_CALL_DROPPED | STATE=DEPLOYED | VERSION=0adaab0847ef | NOTE=Dropped the legacy gpt-5.6-luna analyzeServiceSearch call per COST_REPORT. Verified safe to remove: Serper search path does NOT use requiredProviderCapabilities (grep empty), its only consumer (legacy openai-provider-discovery) is disabled (OPENAI_DURABLE_PROVIDER_RUNTIME_ENABLED=true), and searchPolicy has zero readers. Replaced the model call with its existing rule-based fallback buildRuleBasedSearchAnalysis (same payload shape for admin, zero API cost, no search/specialty change). Cost/request drops one of two model calls ≈ 40% (~$0.018→~$0.011; exact $ pending gpt-5.6-luna pricing, but structurally one fewer model call). VERIFIED on production via canary, 5 varied requests all correct 5/5: قاعة أفراح→PLACE، فني تكييف سبليت→SERVICE، اديتر كامري→PRODUCT، متجر عطور أونلاين→ONLINE_STORE، محامي قضايا عمالية→SERVICE. gpt-4o kept for understanding. Updated 4 stale test pins (ux-6a vision, capsec ordering, ai-search, v1-segment) to the rule-based reality. tsc=0, build PASS, full suite baseline (57) | NEEDS_OWNER=none. Next: Phase C — 7 PHONE_IDENTITY, 8 SCALE_25 (needs Render), 9 DELETE_ACCOUNT, 10 bridge (needs your deploy)
TS=2026-09-05T09:45:00Z | TASK=DELETE_ACCOUNT_FIX_LIVE | STATE=DEPLOYED | VERSION=8148ea564c1a | NOTE=Item 9 (privacy hole closed). Account purge (purgeApplicationAccount) anonymized users + deleted media/sessions but NEVER deleted customer_phone_identities, where the plaintext phone_e164 (durable) lives — a deleted account kept a recoverable number. Now the purge DELETE FROMs customer_phone_identities scoped to (tenant,user) inside the same atomic db.batch; the account_deletion_receipts row in that batch is the audit record. tsc=0, build PASS, full suite baseline (57), +1 test | NEEDS_OWNER=none. Next: SECURITY_AUDIT (owner marked critical + launch-blocking, before rest of queue), then OWNER_DASHBOARD_V2, then Phase C remainder (PHONE_IDENTITY via pairing-as-identity, SCALE_25 [Render], bridge)
TS=2026-09-05T10:10:00Z | TASK=SCALE_25_READY | STATE=ANALYSIS | VERSION=n/a | NOTE=Render plan confirmed: Standard $25 (2GB/1CPU, Frankfurt). Cannot measure real RAM myself (bridge /health is auth-gated 401, no Render shell) — measurement is owner-run. RAM math for 2GB: baseline ~150MB + 250MB spike headroom → 1,648MB for sessions → safe threshold ≈ 66MB/session (25× ). <66MB comfortable, 66-74 borderline→Pro, >74 needs Pro. Baileys typical ~40-60MB/session → Standard LIKELY fits 25 (25×55+150≈1,525MB≈74%, ~500MB headroom) — confirm by measure. Pro (4GB/$85) handles 25 easily. LOAD_TEST method: pair-only (no send) 5→15→25, read node.memory.rss_bytes from GET /health (bridge key) or Render RAM, per-session=(rss@25-rss@5)/20; Standard safe if rss@25<~1.8GB. Three Worker caps to raise IN LOCKSTEP with BRIDGE_MAX_SESSIONS (else Worker admits beyond bridge): CUSTOMER_READY_REAL_SESSION_LIMIT (real-session-capacity.ts:3 =5), MAX_GLOBAL_HEAVY_OPERATIONS (global-heavy-operation-gate.ts:13 + limits.ts =10), MAX_REAL_ACTIVE_WHATSAPP_SESSIONS (OFFICIAL_RELEASE_SNAPSHOT). Did NOT raise caps yet (measure-before-raise + lockstep coordination). Decision: Standard likely enough; start 15, measure, then 25 | NEEDS_OWNER=run pair-only load test 5→15→25, report rss_bytes each stage; then tell me the target and I deploy Worker caps as you set BRIDGE_MAX_SESSIONS
TS=2026-09-05T10:40:00Z | TASK=BRIDGE_DEPLOY_TARGET | STATE=CONFIRMED | VERSION=bridge-b6c63c8 | NOTE=Deploy b6c63c8 (b6c63c8fbbf2262a5ba3ce1f9b2ec0863796f673) = tip of origin/codex/dabbir-render-staging-bridge-v1. Verified from git: PAIRING_BROWSER=Browsers.macOS("Chrome") (server.mjs:216, used in makeWASocket 1966/1983); companionBrowserIdentity call-count=0 (buggy fn removed); LOGOUT+BRIDGE_WIPE_SESSIONS_ON_BOOT+PAIRING_DISK_CLEAN present; signature-auth diff vs working 94cb151 is EMPTY (no regression); e00c536 is ancestor (b6c63c8 newer, includes it). "دبّر" in b6c63c8 is only a DEAD DEFAULT_COMPANION_NAME const + comments, NOT the active tuple. DO NOT deploy e00c536: line 208 calls companionBrowserIdentity(companionName) → browser[1]="دبّر" = the pairing bug (Render shows e00c536 as top prior deploy but it carries the bug). Safe: 94cb151(working) is ancestor of b6c63c8, diff is fixes only | NEEDS_OWNER=deploy b6c63c8 on Render when ready; verify pairing still works after
TS=2026-09-05T10:55:00Z | TASK=SECURITY_AUDIT_REPORT | STATE=DONE | VERSION=n/a | NOTE=3 parallel audits complete. VERDICT: NO CRITICAL — launch not blocked by an exploitable vuln. Strong+consistent: cross-tenant isolation (every request read/write fenced by tenant_id from verified JWT, no IDOR; sec01 passes 7/7), bridge auth (HMAC both directions + TLS + nonce + identity, no impersonation), secrets hygiene (none in code/git/logs; config has only SHA256; keys are Cloudflare secrets), live-session (raw Baileys creds never reach Worker; encrypted at rest on bridge; no raw message storage), admin accessKey (64-hex + real AAL2 admin auth behind it; /api/admin doesn't rely on slug), no-OTP model (anon vs signed-in separate tenants; provider tokens signed/bound). MEDIUM (defense-in-depth, not active exploit): M1 [OWNER] prod AKH_BRIDGE_IDENTITY_SHA256 = copied staging value → verify all bridge secrets differ staging↔prod (esp WHATSAPP_BRIDGE_WEBHOOK_SECRET) + re-render prod identity, do with b6c63c8 deploy; S1 no app CSP + Supabase tokens not httpOnly → add CSP+Referrer-Policy carefully; F1 read-fencing not test-guarded → add regression test. LOW (logged): constant-time slug, slug in returnTo, 302vs404 oracle, /control no-store, secure-cookie env-dependent, directory-lookup unfenced join. LAUNCH TODO: revert anonymous CAP_CEILING 100→50/env5 before public launch (owner's own TODO, still testing) | NEEDS_OWNER=(1) M1 verify per-env bridge secret distinctness [Cloudflare secrets, only you] (2) say GO to implement S1(CSP) + F1(read-fence test) — I'll do them carefully; (3) revert cap before public launch
TS=2026-09-05T11:41:00Z | TASK=SEND_TRIGGER_FIX | STATE=COMMITTED (deploy pending owner) | VERSION=230c767 | NOTE=THE send-after-pairing blocker, root-caused from production D1 (your test req_6e2cc4be…). Pairing reached CONNECTED and auto-dispatch DID fire, but over the whole 6.5-min connected window 18 durable_send_intents were created with EVERY one at attempt_count=0 and dispatch_started_at=NULL — not one message left; job ended BLOCKED_SESSION dispatched=0 failed=0. Cause: dispatchOne runs its durable quote-prep crypto (~2s) for EVERY provider before the PREPARE_ONLY return, and prepared-dispatch prepared ALL ~20 providers before sending even one. At the 20-provider cap (MEGA_UX) that made every step O(providers), overrunning the invocation budget on the webhook auto-dispatch, the paced continuation AND the cron sweep alike, so the send loop never ran. The bridge "PROVIDER_EVENT ERROR" was a Baileys event, not a send — no send was ever attempted. FIX: prepare only what each step sends (sendBudget: 1/step, ≤5/batch); each step is now prepare-one→send-one and the continuation chain + cron carry the rest. Send cap unchanged (no over-send); a failed prep doesn't burn the budget. Tests: full suite 2446 pass / 57 known-baseline / ZERO new; capsec-06 + staging-e2e-send-protection updated to the bounded contract (real invariants preserved). Build clean. Committed + pushed to origin/archive/release-candidate-2026-09-03 | NEEDS_OWNER=DEPLOY IT — this restored session workspace lost the gitignored rendered config + your signed 8-key acceptance manifest (physical-proof), so I could not run the final wrangler deploy from here. In your deploy workspace: (1) git pull  (2) npm run build  (3) npx wrangler deploy --config dist/server/wrangler.production.json  (4) curl -s https://app.dabbir.app/api/build-identity (build hash should change from 8148ea564c1a). This ships SEND_TRIGGER + every other item committed tonight in one deploy. Then re-pair on b6c63c8 and confirm the send fires within seconds of CONNECTED.
TS=2026-09-05T12:05:00Z | TASK=SECURITY_S1_F1 | STATE=COMMITTED (deploy pending) | VERSION=1abb5c0 | NOTE=S1: the app ALREADY ships a least-privilege CSP (script/frame-src scoped to Cloudflare Turnstile, connect-src scoped to the configured Supabase origin+wss, object-src none, frame-ancestors none, base-uri self, form-action self) plus Referrer-Policy strict-origin-when-cross-origin, HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff and Permissions-Policy on EVERY response (worker/index.ts) — the audit's "no CSP" was stale. Added tests/security-response-headers.test.mjs to lock in the full header set so none silently drops, keep connect-src non-wildcard, and keep geolocation=(self) for the map picker. F1: fenced the one unfenced tenant read the audit flagged — directory-lookup's category read joined request_execution_jobs→purchase_requests by job.id alone; now fenced by tenant_id on both tables with the sole caller passing the verified actor's tenant; tests/read-fence-tenant-isolation.test.mjs guards it (provider_registry stays cross-tenant by design). New tests 6/6; provider-directory e2e still 5/5 | NEEDS_OWNER=none (ships with the deploy). Note: Supabase tokens in localStorage (not httpOnly) is a Supabase-SDK design choice; the CSP is its XSS mitigation and is the reason S1's header guard matters
TS=2026-09-05T12:15:00Z | TASK=M1_BRIDGE_SECRET_AUDIT | STATE=ANALYSIS (owner action) | VERSION=n/a | NOTE=Compared staging↔production configs. CONFIRMED shared: AKH_BRIDGE_IDENTITY_SHA256 is byte-identical (44e06eb4…5870f0) in wrangler.staging.jsonc AND wrangler.production.jsonc → the AKH_BRIDGE_IDENTITY secret is the SAME in both environments (a staging compromise could impersonate prod). Also shared: OPENAI_MINIMAL_PROBE_TOKEN_SHA256, REAL_AI_PRECISION_TOKEN_SHA256 (internal probe/canary token guards), NEXT_PUBLIC_TURNSTILE_SITE_KEY (public), AKH_DEPLOYMENT_ARTIFACT_SHA256 (build provenance, not a secret). The true bridge secrets (WHATSAPP_BRIDGE_WEBHOOK_SECRET, _API_KEY, _REQUEST_SIGNING_SECRET, WHATSAPP_SESSION_ENCRYPTION_KEY) are Cloudflare-encrypted so their VALUES can't be compared from here — owner verifies. I did NOT generate/rotate any secret (never handle secret material). Full regeneration checklist in NEEDS_OWNER §2 | NEEDS_OWNER=regenerate AKH_BRIDGE_IDENTITY for prod (set on Cloudflare worker + matching value on Render bridge + update the SHA256 in config), and verify/rotate the 4 WHATSAPP_BRIDGE_* / session secrets so prod ≠ staging — see NEEDS_OWNER §2 for exact steps
TS=2026-09-05T12:25:00Z | TASK=UNDERSTANDING_VISIBILITY | STATE=COMMITTED (deploy pending) | VERSION=53181cf | NOTE=The owner tested "حفلات"/"عطور" and saw no smart question, and no store/place distinction. Root cause (a): understandRequest let a CONFIDENT model (clarify=null) override the curated rule-based ambiguity, so bare حفلات/عطور never asked. Fixed: ruleBased.clarify is now authoritative — it only fires with no strong signal AND no prior answer, so a clear request ("قاعة أفراح") never pauses and an answered ambiguity is never re-asked; +2 tests with a confident-model mock. (b): the results screen now shows "فهمنا طلبك: <searchSubject>" + a type chip (🏛️ مكان/قاعة · 🔧 مزوّد خدمة · 🛍️ محل قريب · 📦 متجر أونلاين), captured from the create response (server-only understanding). So the customer FEELS the intelligence: a buttoned question when ambiguous, and a clear "here is what we understood" on results. tsc clean; full suite baseline (57), +8 net new tests | NEEDS_OWNER=after deploy, on /go type «حفلات» and «عطور» (must ask, buttons) and confirm the results show the type chip; a clear «قاعة أفراح» must NOT ask
TS=2026-09-05T12:30:00Z | TASK=SECRET_SCAN_CLEAN | STATE=DONE | VERSION=n/a | NOTE=Full scan (git history + tree + client bundle dist/client 94 files + API responses + logs). No real secret VALUE leaks anywhere: all key refs are env bindings, .env.example placeholders, SHA256 hashes, IDs, or error-code enums; the deep-understanding prompt is absent from dist/client (server-only). git pickaxe found no commit that ever added an OpenAI/whsec/bridge secret value. dist/ + .env* gitignored. Detail in repo docs/overnight-2026-09-05/SECURITY_SCAN_2026-09-05.md | NEEDS_OWNER=none
TS=2026-09-05T12:33:00Z | TASK=SERVER_SIDE_LOCKED | STATE=DONE | VERSION=n/a | NOTE=All 5 sensitive artifacts server-only (understanding prompt, value/ranking formula, search filtering, site: multi-source trick, phone-extraction regex+prompt) — zero in any client chunk or "use client" component. Customer /go responses are result-only: request-detail returns {name,rating,reviewCount,district} with an explicit "phone is never customer-facing" invariant; search returns status+counts; subject/intentType/phone are server→server only. Minor note: a lightweight deterministic category classifier (intake.ts) is bundled client-side to draft the WhatsApp message — carries no formula/prompt/trick, low sensitivity. Detail in docs/overnight-2026-09-05/SECURITY_SCAN_2026-09-05.md | NEEDS_OWNER=none
TS=2026-09-05T12:36:00Z | TASK=ARCHITECTURE_MAP | STATE=DONE | VERSION=n/a | NOTE=~179k LOC, 3 tiers with the direction actually enforced (modules never import server; server imports app once). Auth/tenant/DB through one AppActor (108 importers). Data layer = RAW D1 (not drizzle at runtime): 1,273 raw SQL sites in 95 files, no data-access layer — a column rename is an unchecked grep. God-files: control-plane.ts 12,312 lines, tier1-jit-saga 8,653, durable-provider-invocation 5,569; god-functions of 844–1,740 lines. DEAD: app/components/new-request-composer.tsx (2,292 lines) — safe to delete. Two features both named "negotiation". Top-3 risks + split plan in docs/overnight-2026-09-05/ARCHITECTURE_MAP.md | NEEDS_OWNER=none (refactor backlog; nothing blocking)
TS=2026-09-05T12:39:00Z | TASK=SCALE_RESILIENCE | STATE=DONE | VERSION=n/a | NOTE=Under a spike the platform degrades gracefully (202/503 + retry-after + Arabic "busy"), every external call timeout-bounded + fail-soft, no crash/hang paths. Bottleneck order: OpenAI discovery ~2-3 concurrent (masked by directory-first + Serper cache) → bridge 5 sessions → heavy-op gate 10 → Serper ~40/day/isolate → anon cap → D1. GAPS: (1) the declared RATE_LIMIT flags have ZERO code readers — no in-worker throttle on request-create/pairing (only resource gates); (2) anon cap shipped 100 not 5 (your TODO); (3) Turnstile fails OPEN if unconfigured — verify TURNSTILE_SECRET_KEY set; (4) Serper budget per-isolate/in-memory (not a durable cost cap); (5) heavy-op slots not partitioned by type. Full report + numbers in docs/overnight-2026-09-05/SCALE_RESILIENCE.md | NEEDS_OWNER=(1) verify TURNSTILE_SECRET_KEY is set in prod; (2) revert anon cap to 5 before public launch; (3) decide whether to wire per-IP rate limiting / CF WAF
TS=2026-09-05T12:45:00Z | TASK=PRICING_SYSTEM_DESIGN + UNIFIED_IDENTITY_DESIGN | STATE=DESIGN (review) | VERSION=n/a | NOTE=Full design of pricing/plans + identity. Prices: request 3.99 هللة, negotiate +6.99 ر, Plus 29.99 (9 req+2 neg), Pro 54.99 (15+5), Free 3 trial. Identity model (answers PHONE_IDENTITY item 7): FREE = no identity, device-bound trial credit; PAID = the PHONE is the identity via a WhatsApp code (pairing-as-identity, no separate OTP), plan bound in customer_phone_identities. Counting: request_credit_accounts keyed by the phone-derived customerId; monthly allowance from an editable plan_catalog table; counter "5 من 9". UNIFIED_IDENTITY: ONE D1 source of truth (the number) — web + iOS are windows on the same subscription row; partial-unique index enforces one ACTIVE sub per number, external_ref prevents double-charge; before any payment (web/Apple) a guard blocks double-pay and shows the active plan; renew/cancel from any channel; 2 numbers = 2 accounts. Multi-channel pay (WEB_GATEWAY ~3% / APPLE_IAP 15-30% / WEB_ONLY), payment deferred ("قريبًا"). Docs: PRICING_SYSTEM_DESIGN.md + UNIFIED_IDENTITY_DESIGN.md | NEEDS_OWNER=review the two design docs; approve before I build the plan_catalog migration + /plans + owner panel
TS=2026-09-05T12:48:00Z | TASK=DISCOUNT_CODES_DESIGN | STATE=DESIGN (review) | VERSION=n/a | NOTE=Owner-panel discount codes: discount_codes table (type percent/fixed, applies_to request/plan/negotiation, max_uses, per_user_limit, expires_at, active) + discount_code_redemptions (atomic used_count + per-user UNIQUE prevents abuse). Server-only validate/redeem (fail-soft, atomic counter). Owner panel: create/edit/disable + usage tracking (which customer used which code). Doc: DISCOUNT_CODES_DESIGN.md | NEEDS_OWNER=review; build with pricing
TS=2026-09-05T12:51:00Z | TASK=NEGOTIATION_DESIGN | STATE=DESIGN (review) | VERSION=n/a | NOTE=Full "فاوض عني" design: after replies → button + card → AI asks the goal by buttons (أقل سعر/أقرب موعد/سعر محدد/ضمان) → gpt-4o negotiates each provider in polite Arabic toward the goal for 2h → live board (countdown + per-provider status "اللؤلؤة: نزلت لـ1300 ✓") → best offer. Pre-pairing choice "الربط لكم؟" [20د إرسال] [ساعتان إرسال+تفاوض] added to /go; "/negotiation" learn-more page with real WhatsApp screenshots. Tech: PREMIUM_NEGOTIATION 2h capacity, per-provider 3-4 msg cap, resumption-chain/cron (no open browser), structured summary only (no raw storage). Behind NEGOTIATION_ADDON_RUNTIME_ENABLED, paid (Pro / +6.99). Doc: NEGOTIATION_DESIGN.md | NEEDS_OWNER=review + 4 decisions (msg cap, tone, provider transparency, early-stop); build after initial launch
TS=2026-09-05T12:54:00Z | TASK=IOS_APP_PLAN + DEV_DOCS_READY | STATE=DONE (plan+docs) | VERSION=n/a | NOTE=IOS_APP_PLAN: React Native + Expo (reuse Worker API + mobile-contracts, native UI, NOT a WebView wrapper which Apple rejects). Pairing code shown natively + native Turnstile SDK; Push for provider replies is the real native value that satisfies Apple; in-app account deletion + privacy already server-side. ~6-8 weeks incl. Apple review; Apple Developer $99/yr (owner registers); recommend AFTER web launch, start Apple account early. DEV_DOCS: DEV_ONBOARDING.md (overview, local run, structure, where-each-feature-lives, journey flow, env vars, how-to-add-a-feature-safely, known traps). Both in docs/overnight-2026-09-05/ | NEEDS_OWNER=IOS: register Apple Dev account? decide IAP vs web-only payment; confirm "after launch"
